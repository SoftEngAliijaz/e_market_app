import 'package:flutter/material.dart';

class ProductsByCurrentAdmin extends StatelessWidget {
  const ProductsByCurrentAdmin({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ProductsByCurrentAdmin'),
      ),
      body: Center(
        child: Text('ProductsByCurrentAdmin'),
      ),
    );
  }
}
