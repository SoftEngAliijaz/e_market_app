import 'package:flutter/material.dart';

class Pridy extends StatelessWidget {
  const Pridy({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('products'),
      ),
      body: Center(
        child: Text('products'),
      ),
    );
  }
}
